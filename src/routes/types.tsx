// import { Component, FunctionComponent, FunctionComponentElement, ReactType, ElementType, ReactNode, ComponentFactory, ComponentState } from 'react';

export type RouteType = {
    path: string;
    component: any;
};

export type RoutesType = Array<RouteType>;
